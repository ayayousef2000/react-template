# ============================================================
#  .npmrc
#
#  Auth and registry settings only.
#  All pnpm-specific settings live in pnpm-workspace.yaml.
#
#  Docs: https://pnpm.io/npmrc
# ============================================================

# Fail loudly if Node or pnpm version does not satisfy "engines" in package.json.
# Mirrored in pnpm-workspace.yaml → engineStrict for pnpm v10+ compatibility.
engine-strict=true
